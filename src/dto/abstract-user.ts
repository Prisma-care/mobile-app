export abstract class AbstractUser {
  // personal info
  id: string;
  firstName: string;
  lastName: string;
  dateOfBirth: Date;
  birthPlace: string;
  updatedAt: Date;
  createdAt: Date;
  // profile picture
  location: string;
}
